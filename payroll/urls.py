from django.conf.urls import url

from . import views

urlpatterns = [
    # url(r'^date/range/$', views.date_range, name='date_range'),
    #
    # url(r'^employee/(?P<employee_id>\d+)/attendance/$', views.employee_attendance, name='employee_attendance'),
    # url(r'^employee/(?P<employee_id>\d+)/month/(?P<month>\d+)/attendance/$', views.employee_attendance_for_month, name='employee_attendance_for_month'),
    #
    # url(r'^month/(?P<month>\d+)/attendance/$', views.month_attendance, name='month_attendance'),
]
